{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 80"
  },
  "devDependencies": {
    "vite": "^4.0.0"
  },
  "dependencies": {
    "dotenv": "^16.1.4",
    "openai": "^3.2.1"
  }
}
